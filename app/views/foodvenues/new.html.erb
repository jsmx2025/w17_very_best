<!-- Validation failure messages -->
<% if @foodvenue.errors.any? %>
  <% @foodvenue.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Foodvenue</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_foodvenue" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for food_id -->
      <div class="form-group">
        <label for="food_id" class="control-label">
          Food
        </label>

        <%= select_tag(:food_id, options_from_collection_for_select(Food.all, :id, :name, @foodvenue.food_id), :class => "form-control") %>
      </div>

      <!-- Label and input for venue_id -->
      <div class="form-group">
        <label for="venue_id" class="control-label">
          Venue
        </label>

        <input type="text" id="venue_id" name="venue_id" placeholder="Venue" class="form-control" value="<%= @foodvenue.venue_id %>">
      </div>

      <button class="btn btn-success">
        Create Foodvenue
      </button>
      or
      <a href="/foodvenues">Cancel</a>
    </form>
  </div>
</div>
